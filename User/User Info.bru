meta {
  name: User Info
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/users/usebrun?key=value
  body: text
  auth: none
}

params:query {
  key: value
}

body:text {
  Hello
}

tests {
  test("status must be 200", function() {
    expect(res.status).to.eql(201);
  });
}
